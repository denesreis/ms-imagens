spring:
  # PostgreSQL (Local)
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:msimagens}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 1200000

  # JPA
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Flyway
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    validate-on-migrate: true

# Logging detalhado em desenvolvimento
logging:
  level:
    com.scasistemas.msimagens: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# Swagger habilitado em dev
springdoc:
  swagger-ui:
    enabled: true

# Actuator aberto em dev
management:
  endpoint:
    health:
      show-details: always

# Cloudflare credentials para desenvolvimento local
app:
  cloudflare:
    account-id: 8e858ade7b52abbd9f51c3071f0cbf42
    get-token: uiXHR6nRCvOmsEuJ7Rm0Pv530NS7A_QaYr38KxbN
    post-token: zXnfAoOiLUHdFJhLxjhJW7c9nGxlxX3BduO2IX2Y
